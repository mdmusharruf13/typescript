const num: number = 98;
const num2 = 598; // this is better than the 'num'

const isActive: boolean = true;

console.log(num, isActive);

type Num = number;
type Bool = boolean;

const num3: Num = 58;

const isActiveUser: Bool = false;

console.log(num3, isActiveUser);
